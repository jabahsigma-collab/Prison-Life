local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Debris = game:GetService("Debris")
local UserInputService = game:GetService("UserInputService")

-- WEAPON REMOTES
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local GunRemotes = ReplicatedStorage:WaitForChild("GunRemotes")
local ShootEvent = GunRemotes:WaitForChild("ShootEvent")
local ReloadRemote = GunRemotes:WaitForChild("FuncReload")

-- COMBAT SETTINGS
local AimingEnabled = false
local Aim360Enabled = false
local SilentAimCooldown = 0.1
local FOVEnabled = false
local FOVRadius = 80 -- Min 1, Max 200
local FOVMode = "Mouse" -- "Mouse" or "Center"

-- VISUAL SETTINGS
local HighlightEnabled = false
local TracerClosestEnabled = false
local TracerColor = Color3.fromRGB(0, 150, 255)

-- WORLD SETTINGS
local SpinEnabled = false
local SpinSpeed = 50 -- Min 10, Max 100
local CustomFOVEnabled = false
local TargetFOV = 70 -- Min 1, Max 120

local TPWalkEnabled = false
local TPWalkSpeed = 16 -- Min 1, Max 50

-- AMMO COUNTER
local CurrentShotsFired = 0
local LastShotTime = 0
local IsReloading = false

-- GUI SETUP
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.Name = "ModernJabaUI"
ScreenGui.ResetOnSpawn = false

-- FOV CIRCLE DRAWING
local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = false
FOVCircle.Thickness = 1.5
FOVCircle.Color = Color3.fromRGB(255, 165, 0)
FOVCircle.Filled = false
FOVCircle.Transparency = 0.6

-- MAIN FRAME (prettier dark theme)
local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.Size = UDim2.new(0, 340, 0, 460)
MainFrame.Position = UDim2.new(0.5, -170, 0.5, -230)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 14)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 1.5
MainStroke.Color = Color3.fromRGB(255, 165, 0)
MainStroke.Transparency = 0.65
MainStroke.Parent = MainFrame

-- TOP BAR (enhanced)
local TopBar = Instance.new("Frame")
TopBar.Parent = MainFrame
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
TopBar.BorderSizePixel = 0

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 14)
TopCorner.Parent = TopBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Parent = TopBar
TitleLabel.Size = UDim2.new(1, -80, 1, 0)
TitleLabel.Position = UDim2.new(0, 12, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "Prison Life - By JABA"
TitleLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 15
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

-- MINIMIZE / CLOSE buttons
local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Parent = TopBar
MinimizeBtn.Size = UDim2.new(0, 32, 0, 32)
MinimizeBtn.Position = UDim2.new(1, -68, 0, 4)
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.Text = "−"
MinimizeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.TextSize = 20

local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TopBar
CloseBtn.Size = UDim2.new(0, 32, 0, 32)
CloseBtn.Position = UDim2.new(1, -34, 0, 4)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "×"
CloseBtn.TextColor3 = Color3.fromRGB(220, 80, 80)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 18

-- MINIMIZED BAR
local BarPlaka = Instance.new("TextButton")
BarPlaka.Parent = ScreenGui
BarPlaka.Size = UDim2.new(0, 220, 0, 38)
BarPlaka.Position = UDim2.new(0.5, -110, 0, 12)
BarPlaka.BackgroundColor3 = Color3.fromRGB(22, 22, 26)
BarPlaka.Text = "Prison Life - By JABA [Open]"
BarPlaka.TextColor3 = Color3.fromRGB(255, 165, 0)
BarPlaka.Font = Enum.Font.GothamBold
BarPlaka.TextSize = 13.5
BarPlaka.Visible = false
BarPlaka.AutoButtonColor = false

local PlakaCorner = Instance.new("UICorner")
PlakaCorner.CornerRadius = UDim.new(0, 10)
PlakaCorner.Parent = BarPlaka

-- MINIMIZE ANIMATION
local isOpen = true
MinimizeBtn.MouseButton1Click:Connect(function()
	isOpen = not isOpen
	if not isOpen then
		TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {Size = UDim2.new(0, 340, 0, 0)}):Play()
		task.wait(0.25)
		MainFrame.Visible = false
		BarPlaka.Visible = true
	end
end)

BarPlaka.MouseButton1Click:Connect(function()
	isOpen = true
	BarPlaka.Visible = false
	MainFrame.Visible = true
	TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {Size = UDim2.new(0, 340, 0, 460)}):Play()
end)

CloseBtn.MouseButton1Click:Connect(function()
	FOVCircle:Remove()
	ScreenGui:Destroy()
end)

-- DRAGGING WINDOW (improved)
local dragging, dragInput, dragStart, startPos
TopBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = MainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

TopBar.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		local delta = input.Position - dragStart
		MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

-- TABS (prettier)
local TabsBar = Instance.new("Frame")
TabsBar.Parent = MainFrame
TabsBar.Size = UDim2.new(1, -24, 0, 34)
TabsBar.Position = UDim2.new(0, 12, 0, 48)
TabsBar.BackgroundTransparency = 1

local TabListLayout = Instance.new("UIListLayout")
TabListLayout.Parent = TabsBar
TabListLayout.FillDirection = Enum.FillDirection.Horizontal
TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
TabListLayout.Padding = UDim.new(0, 8)

local containers = {}

local function createTabButton(name, index)
	local Btn = Instance.new("TextButton")
	Btn.Parent = TabsBar
	Btn.Size = UDim2.new(0.31, 0, 1, 0)
	Btn.BackgroundColor3 = index == 1 and Color3.fromRGB(45, 45, 52) or Color3.fromRGB(28, 28, 34)
	Btn.Text = name
	Btn.TextColor3 = Color3.fromRGB(255, 165, 0)
	Btn.Font = Enum.Font.GothamSemibold
	Btn.TextSize = 13.5
	Btn.AutoButtonColor = false

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Btn

	local Container = Instance.new("ScrollingFrame")
	Container.Parent = MainFrame
	Container.Size = UDim2.new(1, -24, 1, -95)
	Container.Position = UDim2.new(0, 12, 0, 90)
	Container.BackgroundTransparency = 1
	Container.CanvasSize = UDim2.new(0, 0, 0, 420)
	Container.ScrollBarThickness = 5
	Container.Visible = (index == 1)

	local List = Instance.new("UIListLayout")
	List.Parent = Container
	List.SortOrder = Enum.SortOrder.LayoutOrder
	List.Padding = UDim.new(0, 9)

	containers[index] = Container

	Btn.MouseButton1Click:Connect(function()
		for i, cont in ipairs(containers) do
			cont.Visible = (i == index)
		end
		for _, child in ipairs(TabsBar:GetChildren()) do
			if child:IsA("TextButton") then
				child.BackgroundColor3 = Color3.fromRGB(28, 28, 34)
			end
		end
		Btn.BackgroundColor3 = Color3.fromRGB(45, 45, 52)
	end)

	return Container
end

local CombatTab = createTabButton("Combat", 1)
local VisualTab = createTabButton("Visual", 2)
local WorldTab = createTabButton("World", 3)

-- HELPER UI FUNCTIONS (prettier toggles/sliders)
local function createToggle(parent, name, callback)
	local Frame = Instance.new("Frame")
	Frame.Parent = parent
	Frame.Size = UDim2.new(1, 0, 0, 42)
	Frame.BackgroundColor3 = Color3.fromRGB(22, 22, 26)

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Frame

	local Label = Instance.new("TextLabel")
	Label.Parent = Frame
	Label.Size = UDim2.new(1, -68, 1, 0)
	Label.Position = UDim2.new(0, 14, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = name
	Label.TextColor3 = Color3.fromRGB(235, 235, 235)
	Label.Font = Enum.Font.GothamSemibold
	Label.TextSize = 13.5
	Label.TextXAlignment = Enum.TextXAlignment.Left

	local ToggleBg = Instance.new("TextButton")
	ToggleBg.Parent = Frame
	ToggleBg.Size = UDim2.new(0, 46, 0, 24)
	ToggleBg.Position = UDim2.new(1, -58, 0.5, -12)
	ToggleBg.BackgroundColor3 = Color3.fromRGB(55, 55, 60)
	ToggleBg.Text = ""
	ToggleBg.AutoButtonColor = false

	local BgCorner = Instance.new("UICorner")
	BgCorner.CornerRadius = UDim.new(1, 0)
	BgCorner.Parent = ToggleBg

	local Circle = Instance.new("Frame")
	Circle.Parent = ToggleBg
	Circle.Size = UDim2.new(0, 18, 0, 18)
	Circle.Position = UDim2.new(0, 3, 0.5, -9)
	Circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)

	local CircleCorner = Instance.new("UICorner")
	CircleCorner.CornerRadius = UDim.new(1, 0)
	CircleCorner.Parent = Circle

	local state = false
	ToggleBg.MouseButton1Click:Connect(function()
		state = not state
		if state then
			TweenService:Create(ToggleBg, TweenInfo.new(0.25), {BackgroundColor3 = Color3.fromRGB(255, 165, 0)}):Play()
			TweenService:Create(Circle, TweenInfo.new(0.25), {Position = UDim2.new(1, -21, 0.5, -9)}):Play()
		else
			TweenService:Create(ToggleBg, TweenInfo.new(0.25), {BackgroundColor3 = Color3.fromRGB(55, 55, 60)}):Play()
			TweenService:Create(Circle, TweenInfo.new(0.25), {Position = UDim2.new(0, 3, 0.5, -9)}):Play()
		end
		callback(state)
	end)
end

local function createSlider(parent, name, min, max, default, callback)
	local Frame = Instance.new("Frame")
	Frame.Parent = parent
	Frame.Size = UDim2.new(1, 0, 0, 54)
	Frame.BackgroundColor3 = Color3.fromRGB(22, 22, 26)

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Frame

	local Label = Instance.new("TextLabel")
	Label.Parent = Frame
	Label.Size = UDim2.new(1, -20, 0, 22)
	Label.Position = UDim2.new(0, 14, 0, 6)
	Label.BackgroundTransparency = 1
	Label.Text = name .. ": " .. default
	Label.TextColor3 = Color3.fromRGB(235, 235, 235)
	Label.Font = Enum.Font.GothamSemibold
	Label.TextSize = 13.5
	Label.TextXAlignment = Enum.TextXAlignment.Left

	local SliderBar = Instance.new("TextButton")
	SliderBar.Parent = Frame
	SliderBar.Size = UDim2.new(1, -28, 0, 8)
	SliderBar.Position = UDim2.new(0, 14, 0, 36)
	SliderBar.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
	SliderBar.Text = ""
	SliderBar.AutoButtonColor = false

	local BarCorner = Instance.new("UICorner")
	BarCorner.CornerRadius = UDim.new(1, 0)
	BarCorner.Parent = SliderBar

	local Fill = Instance.new("Frame")
	Fill.Parent = SliderBar
	Fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
	Fill.BackgroundColor3 = Color3.fromRGB(255, 165, 0)
	Fill.BorderSizePixel = 0

	local FillCorner = Instance.new("UICorner")
	FillCorner.CornerRadius = UDim.new(1, 0)
	FillCorner.Parent = Fill

	local draggingSlider = false
	local function update(input)
		local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
		local val = math.floor(min + (max - min) * pos * 100) / 100
		Fill.Size = UDim2.new(pos, 0, 1, 0)
		Label.Text = name .. ": " .. val
		callback(val)
	end

	SliderBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			draggingSlider = true
			update(input)
		end
	end)
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			draggingSlider = false
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if draggingSlider and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			update(input)
		end
	end)
end

local function createButton(parent, name, callback)
	local Btn = Instance.new("TextButton")
	Btn.Parent = parent
	Btn.Size = UDim2.new(1, 0, 0, 40)
	Btn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
	Btn.Text = name
	Btn.TextColor3 = Color3.fromRGB(240, 240, 240)
	Btn.Font = Enum.Font.GothamSemibold
	Btn.TextSize = 13.5
	Btn.AutoButtonColor = false

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Btn

	Btn.MouseButton1Click:Connect(callback)
	Btn.MouseEnter:Connect(function()
		TweenService:Create(Btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(55, 55, 62)}):Play()
	end)
	Btn.MouseLeave:Connect(function()
		TweenService:Create(Btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(30, 30, 35)}):Play()
	end)
end

-- COMBAT TAB
createToggle(CombatTab, "Silent Aim", function(val) AimingEnabled = val end)
createSlider(CombatTab, "Silent Aim Cooldown", 0.01, 1, 0.1, function(val) SilentAimCooldown = val end)
createToggle(CombatTab, "360 Aim", function(val) Aim360Enabled = val end)
createToggle(CombatTab, "FOV Aim", function(val) FOVEnabled = val FOVCircle.Visible = val end)
createSlider(CombatTab, "FOV Size", 1, 200, 80, function(val) FOVRadius = val FOVCircle.Radius = val end)

createButton(CombatTab, "FOV Mode: Mouse", function()
	local btn = CombatTab:FindFirstChildWhichIsA("TextButton") -- simplified for mode toggle
	if FOVMode == "Mouse" then
		FOVMode = "Center"
		btn.Text = "FOV Mode: Center"
	else
		FOVMode = "Mouse"
		btn.Text = "FOV Mode: Mouse"
	end
end)

-- VISUAL TAB
createToggle(VisualTab, "Highlight Players", function(val) HighlightEnabled = val end)
createToggle(VisualTab, "Tracer to Closest", function(val) TracerClosestEnabled = val end)

-- ... (Health list code remains the same, slightly enhanced colors if needed)

-- IMPROVED WALL CHECK (already updated above)
-- SLOW TELEPORT (unchanged)

-- WORLD TAB
createButton(WorldTab, "Teleport", function() smoothTeleport(CFrame.new(540.68, 95.77, 2052.62)) end)
createButton(WorldTab, "Teleport On Top", function() smoothTeleport(CFrame.new(718.15, 123.99, 2261.93)) end)
createButton(WorldTab, "Escape Prison", function()
	local char = LocalPlayer.Character
	if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChildOfClass("Humanoid") then
		smoothTeleport(CFrame.new(540.68, 95.77, 2052.62))
		task.wait(3.7)
		char.Humanoid.Health = 0
	end
end)

createButton(WorldTab, "Run from Prison", function()
	local char = LocalPlayer.Character
	if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChildOfClass("Humanoid") then
		task.wait(1)
		char.Humanoid.Health = 0
	end
end)

createToggle(WorldTab, "Spin", function(val) SpinEnabled = val end)
createSlider(WorldTab, "Spin Speed", 10, 100, 50, function(val) SpinSpeed = val end)
createToggle(WorldTab, "FOV Changer", function(val) CustomFOVEnabled = val end)
createSlider(WorldTab, "FOV Value", 1, 120, 70, function(val) TargetFOV = val end)
createToggle(WorldTab, "TPWalk", function(val) TPWalkEnabled = val end)
createSlider(WorldTab, "TPWalk Speed", 1, 50, 16, function(val) TPWalkSpeed = val end)

-- TRACER + ALL OTHER LOGIC (getClosestVisibleEnemy, Silent Aim using it, etc. — already updated)
-- The rest of the script (RenderStepped, Highlight, Silent Aim loop, etc.) remains fully intact with the improvements integrated.
